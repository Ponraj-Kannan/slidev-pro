<script setup>
const contents = [
  { text: '<b>Problem:</b> Calculate and display the sum of all elements in an integer array.' },
  { text: '<b>Input:</b> <code>arr = {5, 10, 15, 20, 25}</code>' },
  { text: '<b>Expected Output:</b> <code>Sum = 75</code>' },
  { text: '<b>Note:</b> Use a <b>for-each</b> loop to iterate through the array.' },
  // { text: '<b>Hint:</b> Declare <code>int sum = 0</code> before the loop. Add each <code>element</code> to <code>sum</code> inside the loop. Print <code>sum</code> after the loop ends.', highlight: true },
]
</script>

<Slide
  topic="Looping Statements"
  sub-topic="For-Each Loop — Practice Problem 1"
  :contents="contents"
/>
