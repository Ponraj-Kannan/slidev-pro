<script setup>
const contents = [
  { text: '<b>Problem:</b> Find and print the largest element in an integer array.' },
  { text: '<b>Input:</b> <code>arr = {3, 17, 8, 42, 11}</code>' },
  { text: '<b>Expected Output:</b> <code>Largest element: 42</code>' },
  { text: '<b>Note:</b> Use a <b>for-each</b> loop. Do not use <code>Arrays.sort()</code>.' },
  // { text: '<b>Hint:</b> Initialize <code>max = arr[0]</code> before the loop. Inside the loop, compare each element with <code>max</code> and update <code>max</code> if the element is greater.', highlight: true },
]
</script>

<Slide
  topic="Looping Statements"
  sub-topic="For-Each Loop — Practice Problem 2"
  :contents="contents"
/>
